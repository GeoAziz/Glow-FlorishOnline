# -----------------------------------------------------------------------------
# Firebase Admin Credentials - REQUIRED FOR SERVER-SIDE OPERATIONS
# -----------------------------------------------------------------------------
# Instructions:
# 1. Go to your Firebase project settings > Service accounts.
# 2. Generate a new private key to download your `service-account-key.json` file if you don't have one.
# 3. Open that JSON file and copy the corresponding values into the variables below.

# Found as "project_id" in your service-account-key.json
FIREBASE_PROJECT_ID=

# Found as "private_key" in your service-account-key.json.
# Copy the entire string, including the "-----BEGIN..." and "-----END..." parts.
# It should be enclosed in double quotes.
FIREBASE_PRIVATE_KEY=

# Found as "client_email" in your service-account-key.json
FIREBASE_CLIENT_EMAIL=
